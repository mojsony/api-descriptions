swagger: '2.0'
x-asee-visibility: public
host: bankapi.net
basePath: /v1/payment
info:
  description: |
    Payment API gives you possibility to create, update, delete and execute balance, credit and cross-border transfers. For each type of transfer there are three types of resources you can use.
    Drafts for working with orders in process of preparation, archive for viewing processed orders and current for orders in process of execution.
    Orders can be authorized using action authorization API. Orders can be created and issued depending on a mandate that customer has on source account.
    Payment API gives you access to a repository of public and personal recipients and also gives you ability to create, update and delete order templates that can be used for fast creation of orders.
    In process of creating orders recipients can be identified by their account or alias associated with recipient in customer’s recipient registry.
    Aliases can be anything, from facebook identity, mobile phone number, e-mail address to a custom value defined by customer himself.
  title: Payment API
  version: v1
  contact:
    name: Nikola Stojanovic
    url: https://bankapi.net/docs/public/payment-getstarted.html
    email: nikola.stojanovic@asseco-see.rs
schemes:
  - https
  - http
tags:
  - description: Working with beneficiaries
    name: Beneficiaries
  - description: Working with drafts and templates
    name: Drafts
  - description: Working with transfers
    name: Transfers
  - description: Working with working days
    name: Working days
  - description: Working with purpose codes
    name: Purpose codes
  - description: Working with classifications
    name: Classifications
consumes:
  - application/json
produces:
  - application/json
paths:
  '/public-beneficiaries':
    get:
      description: |
        Shows list of public beneficiaries.
        It is useful when it's required to see list of public associations whom is payed often.
      operationId: PublicBeneficiaries_GetList
      parameters:
        - $ref: '#/parameters/beneficiary-name-param'
        - $ref: '#/parameters/beneficiary-address-param'
        - $ref: '#/parameters/beneficiary-account-param'
        - $ref: '#/parameters/page-size-param'
        - $ref: '#/parameters/page-param'
        - $ref: '#/parameters/sort-order-param'
        - $ref: '#/parameters/sort-by-param'
        - $ref: '#/parameters/include-param'
        - $ref: '#/parameters/trim-param'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/beneficiary-list'
        default:
          $ref: '#/responses/default-error-response'
      summary: Query beneficiaries from public registry.
      tags:
        - Beneficiaries
      x-asee-tags:
        - Pageable
        - Sortable
        - Shapeable
  '/personal-beneficiaries':
    get:
      description: |
        Shows list of personal beneficiaries.
        It is useful when it's required to see list of persons whom is payed often.
      operationId: PersonalBeneficiaries_GetList
      parameters:
        - $ref: '#/parameters/beneficiary-name-param'
        - $ref: '#/parameters/beneficiary-address-param'
        - $ref: '#/parameters/beneficiary-account-param'
        - $ref: '#/parameters/page-size-param'
        - $ref: '#/parameters/page-param'
        - $ref: '#/parameters/sort-order-param'
        - $ref: '#/parameters/sort-by-param'
        - $ref: '#/parameters/include-param'
        - $ref: '#/parameters/trim-param'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/beneficiary-list'
        default:
          $ref: '#/responses/default-error-response'
      summary: Query beneficiaries from personal registry.
      tags:
        - Beneficiaries
      x-asee-tags:
        - Pageable
        - Sortable
        - Shapeable
    post:
      description: |
        Adds new personal beneficiary.
        It is useful when to have personal beneficiary for someone whom is payed often.
      operationId: PersonalBeneficiaries_Create
      parameters:
        - in: body
          name: cmd
          required: true
          schema:
            $ref: '#/definitions/create-beneficiary-command'
      responses:
        '201':
          description: Created
          schema:
            $ref: '#/definitions/resource-created-result'
        default:
          $ref: '#/responses/default-error-response'
      summary: Add new personal beneficiary
      tags:
        - Beneficiaries
      x-asee-tags: []
  '/personal-beneficiaries/{beneficiary-id}':
    delete:
      description: |
        Deleting personal beneficiary.
        It is useful when it's required to remove someone from personal beneficiaries list, because he/she is not in plan for future payments.
      operationId: PersonalBeneficiaries_Cancel
      parameters:
        - $ref: '#/parameters/beneficiary-id-param'
      responses:
        '204':
          description: No Content
        default:
          $ref: '#/responses/default-error-response'
      summary: Deletes personal beneficiary.
      tags:
        - Beneficiaries
      x-asee-tags: []
    get:
      description: |
        It is useful when details info about one particular beneficiary is required.
      operationId: PersonalBeneficiaries_Get
      parameters:
        - $ref: '#/parameters/beneficiary-id-param'
        - $ref: '#/parameters/include-param'
        - $ref: '#/parameters/trim-param'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/beneficiary'
        default:
          $ref: '#/responses/default-error-response'
      summary: Get personal beneficiary details
      tags:
        - Beneficiaries
      x-asee-tags:
        - Shapeable
    put:
      description: |
        Updating details about personal beneficiary.
        It is useful when some changes are to be made on particular personal beneficiary.
      operationId: PersonalBeneficiaries_Update
      parameters:
        - description: Update beneficiary command
          in: body
          name: cmd
          required: true
          schema:
            $ref: '#/definitions/modify-beneficiary-command'
        - $ref: '#/parameters/beneficiary-id-param'
      responses:
        '204':
          description: No content
        default:
          $ref: '#/responses/default-error-response'
      summary: Modify personal beneficiary
      tags:
        - Beneficiaries
      x-asee-tags: []


  '/drafts':
    get:
      description: |
        Displays list of made payment templates.
        It is useful when it's required to see all payment templates or by some searched criteria.
      operationId: Drafts_GetList
      parameters:
        - $ref: '#/parameters/beneficiary-name-param'
        - $ref: '#/parameters/beneficiary-account-param'
        - $ref: '#/parameters/amount-from-param'
        - $ref: '#/parameters/amount-to-param'
        - $ref: '#/parameters/currency-code-param'
        - $ref: '#/parameters/page-size-param'
        - $ref: '#/parameters/page-param'
        - $ref: '#/parameters/sort-order-param'
        - $ref: '#/parameters/sort-by-param'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/draft-list'
        default:
          $ref: '#/responses/default-error-response'
      summary: Get list of drafts and templates
      tags:
        - Drafts
      x-asee-tags:
        - Pageable
        - Sortable
    post:
      description: |
        Adds new payment template.
        It is useful because it makes easier future similar payments.
      operationId: Drafts_Create
      parameters:
        - description: Initiate to create payment template.
          in: body
          name: cmd
          required: true
          schema:
            $ref: '#/definitions/create-draft-command'
      responses:
        '201':
          description: Created
          schema:
            $ref: '#/definitions/resource-created-result'
        default:
          $ref: '#/responses/default-error-response'
      summary: Add new draft or template
      tags:
        - Drafts
      x-asee-tags: []
  '/drafts/{draft-id}':
    delete:
      description: |
        Deleting particular draft or transfer template.
      operationId: Drafts_Delete
      parameters:
        - $ref: '#/parameters/draft-id-param'
      responses:
        '204':
          description: No Content
        default:
          $ref: '#/responses/default-error-response'
      summary: Delete draft or template
      tags:
        - Drafts
      x-asee-tags: []
    get:
      description: |
        Response contains particular transfer draft or template with all it's details
      operationId: Drafts_Get
      parameters:
        - $ref: '#/parameters/draft-id-param'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/draft'
        default:
          $ref: '#/responses/default-error-response'
      summary: Get details of specific draft or template
      tags:
        - Drafts
      x-asee-tags: []
    put:
      description: |
        Modifies particular payment template.
        It is useful when some changes are required on selected template.
      operationId: Drafts_Modify
      parameters:
        - description: Draft to modify
          in: body
          name: cmd
          required: true
          schema:
            $ref: '#/definitions/modify-draft-command'
        - $ref: '#/parameters/draft-id-param'
      responses:
        '204':
          description: No content
        default:
          $ref: '#/responses/default-error-response'
      summary: Modify draft or template
      tags:
        - Drafts
      x-asee-tags: []
  '/transfers':
    get:
      description: |
        List customer's transfers. This list can be filtered by debtor, creditor, date range, ammount range,
        kind... This is useful when is required to view list of transfers by searched criteria, or all transfers.
      operationId: Transfers_GetList
      parameters:
        - $ref: '#/parameters/beneficiary-account-param'
        - $ref: '#/parameters/transfer-kinds-param'
        - $ref: '#/parameters/transfer-statuses-param'
        - $ref: '#/parameters/date-from-param'
        - $ref: '#/parameters/date-to-param'
        - $ref: '#/parameters/amount-from-param'
        - $ref: '#/parameters/amount-to-param'
        - $ref: '#/parameters/beneficiary-name-param'
        - $ref: '#/parameters/value-date-from-param'
        - $ref: '#/parameters/value-date-to-param'
        - $ref: '#/parameters/currency-code-param'
        - $ref: '#/parameters/description-param'
        - $ref: '#/parameters/latitude-param'
        - $ref: '#/parameters/longitude-param'
        - $ref: '#/parameters/radius-param'
        - $ref: '#/parameters/page-size-param'
        - $ref: '#/parameters/page-param'
        - $ref: '#/parameters/sort-order-param'
        - $ref: '#/parameters/sort-by-param'
        - $ref: '#/parameters/include-param'
        - $ref: '#/parameters/trim-param'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/transfer-list'
        default:
          $ref: '#/responses/default-error-response'
      summary: List transfers
      tags:
        - Transfers
      x-asee-tags:
        - Pageable
        - Sortable
        - Shapeable
  '/transfers/{transfer-id}':
    get:
      description: |
        Gets specific transfer details.
        It is useful when details about particular transfer are required to be seen.
      operationId: Transfers_Get
      parameters:
        - $ref: '#/parameters/transfer-id-param'
        - $ref: '#/parameters/include-param'
        - $ref: '#/parameters/trim-param'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/transfer'
        default:
          $ref: '#/responses/default-error-response'
      summary: Get details of specific transfer
      tags:
        - Transfers
      x-asee-tags:
        - Shapeable
  '/transfers/{transfer-id}/cancel':
    post:
      description: Cancels transfer. It is useful when transfer is abandoned before it is executed
      operationId: Transfers_Cancel
      parameters:
        - $ref: '#/parameters/transfer-id-param'
      responses:
        '202':
          description: Accepted
          schema:
            $ref: '#/definitions/resource-created-result'
        default:
          $ref: '#/responses/default-error-response'
      summary: Cancel transfer
      tags:
        - Transfers
      x-asee-tags: []
  '/transfers/{transfer-id}/image':
    put:
      summary: Set transfer's image url
      description: Sets url of image kept for this transfer in [Content API][content-getstarted.html]
      operationId: Transfers_SetImageUrl
      parameters:
        - description: Set transfer image url command
          in: body
          name: cmd
          required: true
          schema:
            $ref: '#/definitions/set-transfer-image-url-command'
        - $ref: '#/parameters/transfer-id-param'
      responses:
        '204':
          description: No content
        default:
          $ref: '#/responses/default-error-response'
      tags:
        - Transfers
      x-asee-tags: []
  '/transfers/{transfer-id}/geolocation':
    put:
      summary: Set transfer's geolocation
      description: Sets transfer geolocation or update new one instead of previous. It is way to put location where transfer was made, or change previous one, in order to mark specific transfer
      operationId: Transfers_SetGeolocation
      parameters:
        - description: Set transfer geolocation command
          in: body
          name: cmd
          required: true
          schema:
            $ref: '#/definitions/set-transfer-geolocation-command'
        - $ref: '#/parameters/transfer-id-param'
      responses:
        '204':
          description: No content
        default:
          $ref: '#/responses/default-error-response'
      tags:
        - Transfers
      x-asee-tags: []
  '/transfers/balance-transfers':
    post:
      description: |
        Initiates new balance transfer.
        It is step in processing new transfer.
      operationId: BalanceTransfers_Initiate
      parameters:
        - description: Initiate balance transfer command.
          in: body
          name: cmd
          required: true
          schema:
            $ref: '#/definitions/balance-transfer-instruction'
      responses:
        '202':
          description: Accepted
          schema:
            $ref: '#/definitions/resource-created-result'
        '440':
          description: |
            Your request was well constructed but it does not comply with business policy. Consider the following possible problems and look into response for more details:

            [allowed-amount-exceeded](payment-problems.html)
            [blocked-account](payment-problems.html)
            [transfer-not-allowed](payment-problems.html)
            [debtor-card-not-active](payment-problems.html)
            [frequency-limit-exceeded](payment-problems.html)
            [amount-above-limit](payment-problems.html)
            [account-deposited](payment-problems.html)
            [insufficient-available-balance](payment-problems.html)
            [transaction-currency-not-allowed](payment-problems.html)
            [amount-negative-number](payment-problems.html)
            [debtor-account-or-currency-not-valid](payment-problems.html)
            [creditor-account-or-currency-not-valid](payment-problems.html)
            [date-not-valid](payment-problems.html)
          schema:
            $ref: '#/definitions/business-problem'
        default:
          $ref: '#/responses/default-error-response'
      summary: Initiate balance transfer
      tags:
        - Transfers
      x-asee-tags: []
  '/transfers/balance-transfers/validate':
    post:
      description: |
        Validates new balance transfer.
        It is useful because it perform various checks before transfer is accomplished.
      operationId: BalanceTransfers_Validate
      parameters:
        - description: Validate credit transfer command.
          in: body
          name: cmd
          required: true
          schema:
            $ref: '#/definitions/balance-transfer-instruction'
      responses:
        '202':
          description: Accepted
        400:
          $ref: '#/responses/validation-problem-response'
        # '440':
        #   description: |
        #     Your request was well constructed but it does not comply with business policy. Consider the following possible problems and look into response for more details:

        #     [allowed-amount-exceeded](payment-problems.html)
        #     [transfer-not-allowed](payment-problems.html)
        #     [blocked-account](payment-problems.html)
        #     [transfer-not-allowed-accounts-types](payment-problems.html)
        #     [debtor-card-not-active](payment-problems.html)
        #     [frequency-limit-exceeded](payment-problems.html)
        #     [amount-above-limit](payment-problems.html)
        #     [account-deposited](payment-problems.html)
        #     [insufficient-available-balance](payment-problems.html)
        #     [transaction-currency-not-allowed](payment-problems.html)
        #     [creditor-aml-check-failed](payment-problems.html)
        #     [debtor-aml-check-failed](payment-problems.html)
        #     [debtor-account-or-currency-not-valid](payment-problems.html)
        #     [creditor-account-or-currency-not-valid](payment-problems.html)
        #     [date-not-valid](payment-problems.html)
        #   schema:
        #     $ref: '#/definitions/business-problem'
        default:
          $ref: '#/responses/default-error-response'
      summary: Validate balance transfer
      tags:
        - Transfers
      x-asee-tags: []
  '/transfers/credit-transfers':
    post:
      description: |
        Initiates new credit transfer.
        It is step in processing new transfer.
      operationId: CreditTransfers_Initiate
      parameters:
        - description: initiate credit transfer command.
          in: body
          name: cmd
          required: true
          schema:
            $ref: '#/definitions/credit-transfer-instruction'
      responses:
        '202':
          description: Accepted
          schema:
            $ref: '#/definitions/resource-created-result'
        '440':
          description: |
            Your request was well constructed but it does not comply with business policy. Consider the following possible problems and look into response for more details:

            [blocked-account](payment-problems.html)
            [debtor-account-blocked](payment-problems.html)
            [work-date-not-set](payment-problems.html)
            [put-order-on-wait-error](payment-problems.html)
            [debtor-card-not-active](payment-problems.html)
            [frequency-limit-exceeded](payment-problems.html)
            [amount-above-limit](payment-problems.html)
            [payment-date-not-set](payment-problems.html)
            [insufficient-available-balance](payment-problems.html)
            [transaction-currency-not-allowed](payment-problems.html)
            [debtor-account-deposited](payment-problems.html)
            [immediate-executing-order-error](payment-problems.html)
            [amount-negative-number](payment-problems.html)
            [debtor-account-or-currency-not-valid](payment-problems.html)
            [order-validation-failed](payment-problems.html)
            [date-not-valid](payment-problems.html)
          schema:
            $ref: '#/definitions/business-problem'
        default:
          $ref: '#/responses/default-error-response'
      summary: Initiate credit transfer
      tags:
        - Transfers
      x-asee-tags: []
  '/transfers/credit-transfers/validate':
    post:
      description: |
        Validates new credit transfer.
        It is useful because it perform various checks before transfer is accomplished.
      operationId: CreditTransfers_Validate
      parameters:
        - description: Validate credit transfer command.
          in: body
          name: cmd
          required: true
          schema:
            $ref: '#/definitions/credit-transfer-instruction'
      responses:
        '202':
          description: Accepted
        400:
          $ref: '#/responses/validation-problem-response'
        # '440':
        #   description: |
        #     Your request was well constructed but it does not comply with business policy. Consider the following possible problems and look into response for more details:

        #     [frequency-limit-exceeded](payment-problems.html)
        #     [work-date-not-set](payment-problems.html)
        #     [blocked-account](payment-problems.html)
        #     [payment-date-not-set](payment-problems.html)
        #     [amount-above-limit](payment-problems.html)
        #     [insufficient-available-balance](payment-problems.html)
        #     [transaction-currency-not-allowed](payment-problems.html)
        #     [debtor-account-or-currency-not-valid](payment-problems.html)
        #     [date-not-valid](payment-problems.html)
        #   schema:
        #     $ref: '#/definitions/business-problem'
        default:
          $ref: '#/responses/default-error-response'
      summary: Validate credit transfer
      tags:
        - Transfers
      x-asee-tags: []
  '/transfers/cross-border-transfers':
    post:
      description: |
        Initiates new cross-border transfer.
        It is step in processing new transfer.
      operationId: CrossBorderTransfers_Initiate
      parameters:
        - description: Initiate cross border transfer command.
          in: body
          name: cmd
          required: true
          schema:
            $ref: '#/definitions/cross-border-transfer-instruction'
      responses:
        '202':
          description: Accepted
          schema:
            $ref: '#/definitions/resource-created-result'
        '440':
          description: |
            Your request was well constructed but it does not comply with business policy. Consider the following possible problems and look into response for more details:

            [order-reference-not-created](payment-problems.html)
            [wrong-order-statistics](payment-problems.html)
            [amount-negative-number](payment-problems.html)
            [currency-not-valid ](payment-problems.html)
          schema:
            $ref: '#/definitions/business-problem'
        default:
          $ref: '#/responses/default-error-response'
      summary: Initiate cross-border transfer
      tags:
        - Transfers
      x-asee-tags: []
  '/transfers/cross-border-transfers/validate':
    post:
      description: |
        Validates new cross-border transfer.
        It is useful because it perform various checks before transfer is accomplished.
      operationId: CrossBorderTransfers_Validate
      parameters:
        - description: Validate credit transfer command.
          in: body
          name: cmd
          required: true
          schema:
            $ref: '#/definitions/cross-border-transfer-instruction'
      responses:
        '202':
          description: Accepted
        400:
          $ref: '#/responses/validation-problem-response'
        # '440':
        #   description: |
        #     Your request was well constructed but it does not comply with business policy. Consider the following possible problems and look into response for more details:

        #     [frequency-limit-exceeded](payment-problems.html)
        #     [amount-above-limit](payment-problems.html)
        #     [creditor-aml-check-failed](payment-problems.html)
        #     [debtor-aml-check-failed](payment-problems.html)
        #     [debtor-account-not-entered](payment-problems.html)
        #     [creditor-name-not-entered](payment-problems.html)
        #     [creditor-address-not-entered](payment-problems.html)
        #     [creditor-city-not-entered](payment-problems.html)
        #     [creditor-IBAN/account-not-entered](payment-problems.html)
        #     [creditor-SWIFT-not-entered](payment-problems.html)
        #     [creditor-SWIFT-not-valid](payment-problems.html)
        #     [currency-not-entered](payment-problems.html)
        #     [currency-not-valid](payment-problems.html)
        #     [purpose-code-not-entered](payment-problems.html)
        #     [purpose-code-not-valid](payment-problems.html)
        #     [amount-not-entered](payment-problems.html)
        #     [insufficient-balance](payment-problems.html)
        #     [creditor-country-code-not-entered](payment-problems.html)
        #     [creditor-country-code-not-valid](payment-problems.html)
        #   schema:
        #     $ref: '#/definitions/business-problem'
        default:
          $ref: '#/responses/default-error-response'
      summary: Validates cross-border transfer
      tags:
        - Transfers
      x-asee-tags: []
  '/transfers/nonresident-credit-transfers':
    post:
      description: |
        Initiates new nonresident transfer.
        It is step in processing new transfer.
      operationId: NonresidentCreditTransfers_Initiate
      parameters:
        - description: Instruction to initiate nonresident transfer
          in: body
          name: cmd
          required: true
          schema:
            $ref: '#/definitions/nonresident-transfer-instruction'
      responses:
        '202':
          description: Accepted
          schema:
            $ref: '#/definitions/resource-created-result'
        default:
          $ref: '#/responses/default-error-response'
      summary: Initiate nonresident's credit transfer
      tags:
        - Transfers
      x-asee-tags: []
  '/transfers/p2p-transfers':
    post:
      description: |
        Initiates new person to person transfer.
        It is step in processing new transfer.
      operationId: PersonToPersonTransfers_Initiate
      parameters:
        - description: Initiate person to person transfer command.
          in: body
          name: cmd
          required: true
          schema:
            $ref: '#/definitions/p2p-transfer-instruction'
      responses:
        '202':
          description: Accepted
          schema:
            $ref: '#/definitions/resource-created-result'
        '440':
          description: |
            Your request was well constructed but it does not comply with business policy. Consider the following possible problems and look into response for more details:

            [insufficient-available-balance](payment-problems.html)
            [frequency-limit-exceeded](payment-problems.html)
            [amount-above-limit](payment-problems.html)
            [authorization-code-error](payment-problems.html)
            [reservation-error](payment-problems.html)
          schema:
            $ref: '#/definitions/business-problem'
        default:
          $ref: '#/responses/default-error-response'
      summary: Initiate person to person transfer
      tags:
        - Transfers
      x-asee-tags: []
  '/transfers/p2p-transfers/{transfer-id}/execute':
    post:
      description: |
        Executes person to person transfer.
        It is used to finaly confirm that transfer is ready to be executed after check procedures it successfully passed.
      operationId: PersonToPersonTransfers_Execute
      parameters:
        - $ref: '#/parameters/transfer-id-param'
        - description: Execute person to person transfer command.
          in: body
          name: cmd
          required: true
          schema:
            $ref: '#/definitions/execute-p2p-transfer-instruction'
      responses:
        '202':
          description: Accepted
          schema:
            $ref: '#/definitions/resource-created-result'
        '440':
          description: |
            Your request was well constructed but it does not comply with business policy. Consider the following possible problems and look into response for more details:

            [reservation-not-exists](payment-problems.html)
            [transfer-not-allowed-for-nonresident](payment-problems.html)
            [obligation-type-credit-not-defined](payment-problems.html)
            [obligation-type-not-defined](payment-problems.html)
            [event-not-defined](payment-problems.html)
            [creditor-account-deposited](payment-problems.html)
            [credit-event-not-defined](payment-problems.html)
            [creditor-account-or-currency-not-valid](payment-problems.html)
            [wrong-transaction-amount](payment-problems.html)
            [transfer-canceled](payment-problems.html)
          schema:
            $ref: '#/definitions/business-problem'
        default:
          $ref: '#/responses/default-error-response'
      summary: Execute person to person transfer
      tags:
        - Transfers
      x-asee-tags: []
  '/transfers/p2p-transfers/validate':
    post:
      description: |
        Validates new person to person transfer.
        It is useful because it perform various checks before transfer is accomplished.
      operationId: PersonToPersonTransfers_Validate
      parameters:
        - description: Validate person to person transfer command.
          in: body
          name: cmd
          required: true
          schema:
            $ref: '#/definitions/p2p-transfer-instruction'
      responses:
        '202':
          description: Accepted
        400:
          $ref: '#/responses/validation-problem-response'
        # '440':
        #   description: |
        #     Your request was well constructed but it does not comply with business policy. Consider the following possible problems and look into response for more details:

        #     [amount-above-limit](payment-problems.html)
        #     [blocked-account](payment-problems.html)
        #     [insufficient-available-balance](payment-problems.html)
        #     [frequency-limit-exceeded](payment-problems.html)
        #     [transfer-not-allowed](payment-problems.html)
        #   schema:
        #     $ref: '#/definitions/business-problem'
        default:
          $ref: '#/responses/default-error-response'
      summary: Validate person to person transfer
      tags:
        - Transfers
      x-asee-tags: []
  '/transfers/top-up-transfers':
    post:
      description: |
        Initiates new top-up transfer.
        It is step in processing new transfer.
      operationId: Top-upTransfers_Initiate
      parameters:
        - description: Initiate top-up transfer command.
          in: body
          name: cmd
          required: true
          schema:
            $ref: '#/definitions/top-up-transfer-instruction'
      responses:
        '202':
          description: Accepted
          schema:
            $ref: '#/definitions/resource-created-result'
        '440':
          description: |
            Your request was well constructed but it does not comply with business policy. Consider the following possible problems and look into response for more details:

            [pairing-reservations-error](payment-problems.html)
          schema:
            $ref: '#/definitions/business-problem'
        default:
          $ref: '#/responses/default-error-response'
      summary: Initiate top-up transfer
      tags:
        - Transfers
      x-asee-tags: []
  '/transfers/wallet-transfers':
    post:
      summary: Initiate wallet transfer
      description: Initiates new wallet transfer that will either add funds to wallet or withdraw funds to linked current account.
      operationId: WalletTransfers_Initiate
      parameters:
        - description: Initiate wallet transfer command
          in: body
          name: cmd
          required: true
          schema:
            $ref: '#/definitions/wallet-transfer-instruction'
      responses:
        '202':
          description: Accepted
          schema:
            $ref: '#/definitions/resource-created-result'
        '440':
          description: |
            Your request was well constructed but it could not be processed. Consider the following possible problems and look into response for more details:

            - [amount-above-limit](payment-problems.html)
            - [blocked-account](payment-problems.html)
            - [insufficient-available-balance](payment-problems.html)
          schema:
            $ref: '#/definitions/business-problem'
        default:
          $ref: '#/responses/default-error-response'

      tags:
        - Transfers
      x-asee-tags: []
  '/transfers/bill-payments':
    post:
      description: |
        Bill payment solution.
        It is useful because it presents an easy way to pay some bill.
      operationId: BillPayments_Initiate
      parameters:
        - description: Instruction with details to initiate bill payment
          in: body
          name: cmd
          required: true
          schema:
            $ref: '#/definitions/bill-payment-instruction'
      responses:
        '202':
          description: Accepted
          schema:
            $ref: '#/definitions/resource-created-result'
        '440':
          description: |
            Your request was well constructed but it does not comply with business policy. Consider the following possible problems and look into response for more details:

            [blocked-account](payment-problems.html)
            [account-not-active](payment-problems.html)
            [not-marked-for-verification](payment-problems.html)
            [payment-date-not-set](payment-problems.html)
            [insufficient-available-balance](payment-problems.html)
            [funds-reservation-error](payment-problems.html)
          schema:
            $ref: '#/definitions/business-problem'
        default:
          $ref: '#/responses/default-error-response'
      summary: Initiate bill payment
      tags:
        - Transfers
      x-asee-tags: []

  '/transfers/incoming':
    get:
      summary: List incoming cross-border transfers
      description: Response contains list of incomming cross-border transfers. List can be filtered by various criteria as date-from and date-to.
      operationId: IncomingTransfers_GetList
      parameters:
        - $ref: '#/parameters/income-status-param'
        - $ref: '#/parameters/value-date-from-param'
        - $ref: '#/parameters/value-date-to-param'
        - $ref: '#/parameters/currency-code-param'
        - $ref: '#/parameters/sender-param'
        - $ref: '#/parameters/page-size-param'
        - $ref: '#/parameters/page-param'
        - $ref: '#/parameters/sort-order-param'
        - $ref: '#/parameters/sort-by-param'
        - $ref: '#/parameters/include-param'
        - $ref: '#/parameters/trim-param'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/incoming-cross-border-transfer-list'
        default:
          $ref: '#/responses/default-error-response'
      tags:
        - Transfers
      x-asee-tags:
        - Pageable
        - Sortable
        - Shapeable
  '/working-days/{date}':
    get:
      description: Response contains next working day relative to day passed in as parameter. It is useful when it's required to get info about next working day of a bank for some date in the middle of a holiday.
      operationId: WorkingDays_GetNext
      parameters:
        - $ref: '#/parameters/date-param'
        - $ref: '#/parameters/next-param'
        - $ref: '#/parameters/service-param'
        - $ref: '#/parameters/include-param'
        - $ref: '#/parameters/trim-param'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/working-day'
        default:
          $ref: '#/responses/default-error-response'
      summary: Get next working day
      tags:
        - Working days
      x-asee-tags:
        - Shapeable
  '/purpose-codes':
    get:
      description: |
        Response contains list of appropriate purpose codes.
        It is useful when choosing purpose code for some transfer.
      operationId: PurposeCodes_GetList
      parameters:
        - $ref: '#/parameters/purpose-code-param'
        - $ref: '#/parameters/direction-param'
        - $ref: '#/parameters/residency-param'
        - $ref: '#/parameters/payout-currency-param'
        - $ref: '#/parameters/payout-method-param'
        - $ref: '#/parameters/clearing-param'
        - $ref: '#/parameters/cross-border-param'
        - $ref: '#/parameters/page-size-param'
        - $ref: '#/parameters/page-param'
        - $ref: '#/parameters/sort-order-param'
        - $ref: '#/parameters/sort-by-param'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/purpose-code-list'
        default:
          $ref: '#/responses/default-error-response'
      summary: List purpose codes
      tags:
        - Purpose codes
      x-asee-tags:
        - Pageable
        - Sortable
# ****************************************************#
# ENDPOINTS COMMON WITH OTHER APIs                    #
# ****************************************************#

  /classifications:
    get:
      summary: List all classification schemas used by the API
      description: Some fields and parameters have been restricted to a list of allowed lookup values maintained by the bank. This method lists all schemas used in this API.
      operationId: "Classifications_GetList"
      responses:
        '200':
          description: Success
          schema:
            $ref: '#/definitions/classification-list'
        default:
          $ref: '#/responses/default-error-response'
      tags:
        - Classifications

  /classifications/{schema-id}:
    get:
      summary: List values allowed by given classification schema.
      description: Returns all classification values for chosen classification.
      operationId: Classifications_Get
      parameters:
        - $ref: '#/parameters/schema-id-param'
      responses:
        '200':
          description: OK
          schema:
            $ref: '#/definitions/classification'
        default:
          $ref: '#/responses/default-error-response'

      tags:
        - Classifications
parameters:
  schema-id-param:
    name: schema-id
    description: Identifier of classification schema
    in: path
    required: true
    type: string
  page-param:
    name: page
    in: query
    description: Page index. For more information see general guidance on [paging](common-getstarted.html#paging)
    type: integer
    default: 1
    x-asee-common: true
  page-size-param:
    name: page-size
    in: query
    description: Number of items on a page. For more information see general guidance on [paging](common-getstarted.html#paging)
    required: false
    type: integer
    default: 10
    x-asee-common: true
  sort-order-param:
    description: Sort order (`asc` or `desc`). Default is asc. For more information see general guidance on [sorting](common-getstarted.html#sorting)
    in: query
    name: sort-order
    type: string
    default: asc
    enum:
    - asc
    - desc
    x-asee-common: true
  sort-by-param:
    description: Attribute of the collection item to sort by. For more information see general guidance on [sorting](common-getstarted.html#sorting)
    in: query
    name: sort-by
    type: string
    x-asee-common: true
  include-param:
    description: List of fields to include in response. For more information see general guidance on [response shaping](common-getstarted.html#shaping)
    in: query
    name: include
    type: array
    items:
      type: string
    collectionFormat: csv
    x-asee-common: true
  trim-param:
    description: List of fields to trim from response. For more information see general guidance on [response shaping](common-getstarted.html#shaping)
    in: query
    name: trim
    type: array
    items:
      type: string
    collectionFormat: csv
    x-asee-common: true



  beneficiary-name-param:
    description: Name of the beneficiary.
    in: query
    name: name
    required: false
    type: string
  beneficiary-address-param:
    description: Address of the beneficiary.
    in: query
    name: address
    required: false
    type: string
  beneficiary-account-param:
    description: Beneficiary account number.
    in: query
    name: account
    required: false
    type: string
  beneficiary-id-param:
    description: Beneficiary id
    in: path
    name: beneficiary-id
    required: true
    type: string
  purpose-code-param:
    description: Purpose identifier.
    in: query
    name: purpose-code
    required: false
    type: string
  direction-param:
    description: 'Direction of transaction: pay-in, pay-out.'
    in: query
    name: direction
    required: false
    enum:
      - credit
      - debit
    type: string
  residency-param:
    description: Indicator whether the customer is resident or non-resident.
    in: query
    name: residency
    required: false
    enum:
      - resident
      - non-resident
    type: string
  payout-currency-param:
    description: Indicator whether the payout is made in domestic or foreign currency.
    in: query
    name: payout-currency
    required: false
    enum:
      - domestic-currency
      - foreign-currency
    type: string
  payout-method-param:
    description: |
      Indicates the method in which payout is made.
      For a list of possible values see [payout-methods](payment-classifications.html#payout-methods) enumeration.
    in: query
    name: payout-method
    required: false
    enum:
      - cash
      - cheque
    type: string
  clearing-param:
    description: Indicates whether the transaction goes through clearing or not.
    in: query
    name: clearing
    required: false
    type: boolean
  cross-border-param:
    description: Identifies if this is abroad or domestic transaction.
    in: query
    name: cross-border
    required: false
    type: boolean
  draft-id-param:
    description: Draft identifier.
    in: path
    name: draft-id
    required: true
    type: string
  amount-from-param:
    description: Amount from.
    in: query
    name: amount-from
    required: false
    format: double
    type: number
    minimum: 0
  amount-to-param:
    description: Amount to.
    in: query
    name: amount-to
    required: false
    format: double
    type: number
    minimum: 0
  currency-code-param:
    description: |
          ISO3166-1 3 Letter currency code. List of possible currencies is maintained by [Reference API](reference-getstarted.html)
    in: query
    name: currency-code
    required: false
    type: string
    minLength: 3
    maxLength: 3
  transfer-kinds-param:
    description: Kind of the transfer.
    in: query
    name: kinds
    required: false
    enum:
      - balance-transfer
      - credit-transfer
      - cross-border-transfer
      - nonresident-transfer
      - p2p-transfer
      - topup
      - bill-payment
    type: array
    collectionFormat: csv
    items:
      type: string
  transfer-statuses-param:
    description: |
      This parameter is used to filter the result by status of the transfer. Only transfers with specific status are returned.
      For a list of possible values see [transfer-statuses](payment-classifications.html#transfer-statuses) enumeration.
    in: query
    name: statuses
    required: false
    enum:
      - pending
      - scheduled
      - executed
      - rejected
      - canceled
    type: array
    collectionFormat: csv
    items:
      type: string
  date-from-param:
    description: Filters transfers initiated after the specified date.
    in: query
    name: date-from
    required: false
    format: date
    type: string
  date-to-param:
    description: Filters transfers initiated before the specified date.
    in: query
    name: date-to
    required: false
    format: date
    type: string
  value-date-from-param:
    description: Filters transfers where value date is after the specified date.
    in: query
    name: value-date-from
    required: false
    format: date
    type: string
  value-date-to-param:
    description: Filters transfers where value date is before the specified date.
    in: query
    name: value-date-to
    required: false
    format: date-time
    type: string
  description-param:
    description: Filters transfers by description.
    in: query
    name: description
    required: false
    type: string
  latitude-param:
    description: 'Latitude. Filters transfers by geolocation. For this filter all three values should be provided: latitude, longitude, radius.'
    in: query
    name: latitude
    required: false
    format: float
    type: number
  longitude-param:
    description: 'Longitude. Filters transfers by geolocation. For this filter all three values should be provided: latitude, longitude, radius.'
    in: query
    name: longitude
    required: false
    format: float
    type: number
  radius-param:
    description: 'Radius. Filters transfers by geolocation. For this filter all three values should be provided: latitude, longitude, radius.'
    in: query
    name: radius
    required: false
    format: int32
    type: integer
  transfer-id-param:
    description: Transfer identifier.
    in: path
    name: transfer-id
    required: true
    type: string
  income-status-param:
    description: |
      Income status.
      For a list of possible values see [income-statuses](payment-classifications.html#income-statuses) enumeration.
    in: query
    name: status
    required: false
    enum:
      - pending
      - executed
      - in-process
    type: string
  sender-param:
    description: Sender.
    in: query
    name: sender
    required: false
    type: string
  date-param:
    description: Date which is being checked in calendar of non-working days.
    in: path
    name: date
    required: true
    type: string
  next-param:
    description: Indicates if next working day should be returned (if specified date is the non-working one).
    in: query
    name: next
    required: false
    type: boolean
  service-param:
    description: Identifier of service for which working day is being checked.
    in: query
    name: service
    required: false
    type: string
definitions:
  beneficiary-list:
    description: |
      List of beneficiaries. Can be list of public beneficiaries,
      or list of users personal beneficiaries.
    allOf:
    - $ref: '#/definitions/paged-list'
    properties:
      items:
        description: Beneficiary list.
        items:
          $ref: '#/definitions/beneficiary'
        type: array
    type: object
    example:
      total-count: 455
      page-size: 10
      page-number: 1
      total-pages: 46
      items:
      -
        beneficiary-id: TD23235
        name: Name1
        nickname: Nickname1
        address: Resavska br.2, Beograd
        image-url: https://www.benificiaryimages.com/search?q=beneficiary-id&espv=rce=leRDvMlvCGl7M%3A
        is-trusted: true
        accounts:
          - account-number: "160-127-89"
            kind: term-loan-account
            currency: EUR
            bank-name: First Bank
            swift-code: AIKBRS22
            country-code: RS
          - account-number: "160-123-45"
            kind: term-loan-account
            currency: EUR
            bank-name: Foreign Bank
            swift-code: AIKBRS22
            country-code: RS
        identifiers:
          - id: 5584d4sd-8bfc-5741-9b55-a6c0ce6a1558
            type: email
      -
        beneficiary-id: DG23255
        name: Name2
        nickname: Nickname2
        address: Bulevar kralja Aleksandra br.124, Beograd
        image-url: https://www.benificiaryimages.com/search?q=bsdflvCGl7M%3A
        is-trusted: false
        accounts:
          - account-number: "160-322432136-12"
            kind: current-account
            currency: EUR
            bank-name: National Bank
            swift-code: AIKBRS84
            country-code: RS
        identifiers:
          - id: 26b22680-8bfc-4602-9b55-a6c0ce6a1558
            type: email
  beneficiary:
    description: Party which receives transfers
    properties:
      beneficiary-id:
        description: Unique id of beneficiary
        type: string
      name:
        description: Name of beneficiary
        type: string
      nickname:
        description: Nickname of beneficiary
        type: string
      address:
        $ref: '#/definitions/address'
      image-url:
        description: URL of beneficiary image kept in content repository
        type: string
      is-trusted:
        description: Whether beneficiary is trusted or not
        type: boolean
      accounts:
        description: Accounts
        items:
          $ref: '#/definitions/account-info'
        type: array
      identifiers:
        description: Identifiers.
        items:
          $ref: '#/definitions/party-identifier'
        type: array
    type: object
    example:
      beneficiary-id: MD23451
      name: name1
      nickname: nickname1
      address: Bulevar Zorana Djindjica br.346, Beograd
      image-url: https://www.benificiaryimages.com/search?q=bendsfs7M%3A
      is-trusted: true
      accounts:
        - account-number: "160-3453467-122"
          kind: term-loan-account
          currency: EUR
          bank-name: First Bank
          swift-code: AIKBRS23
          country-code: RS
        - account-number: "160-121234-12"
          kind: term-loan-account
          currency: EUR
          bank-name: Foreign Bank
          swift-code: AIKBRS65
          country-code: RS
      identifiers:
        - id: 5584d4sd-8bfc-5741-9b55-a6c0ce6a1558
          type: email
  account-info:
    description: Party's account maintained by a bank
    properties:
      account-number:
        description: Account number
        type: string
      kind:
        description: |
          Kind of account
          For a list of possible values see [account-kinds](payment-classifications.html#account-kinds) enumeration.
        enum:
          - current-account
          - demand-deposit-account
          - term-deposit-account
          - term-loan-account
          - overdraft-account
          - credit-facility-account
          - credit-card-account
          - investment-account
        type: string
      currency:
        description: "ISO 4217 currency code. Complete list of currencies is maintained by [Reference API](reference-getstarted.html)."
        type: string
      bank-name:
        description: Name of the bank.
        type: string
      swift-code:
        description: Swift code of creditor bank. Mostly used for foreign payments.
        type: string
      country-code:
        description: "ISO3166 2 letter code of country. List of countries is maintained by [Reference API](reference-getstarted.html)."
        type: string
    type: object
  party-identifier:
    description: 'Unique identifier of party and it`s type'
    example:
      id: 26b22680-8bfc-4602-9b55-a6c0ce6a1558
      kind: email
    properties:
      id:
        description: Identifier value.
        type: string
      kind:
        description: |
          Kind of identifier
          For a list of possible values see [party-kinds](payment-classifications.html#party-kinds) enumeration.
        enum:
          - facebook
          - google
          - twitter
          - msisdn
          - email
        type: string
    required:
      - id
      - kind
    type: object
  create-beneficiary-command:
    description: Command to create personal beneficiary.
    required:
      - name
      - is-trusted
    properties:
      name:
        description: Name of beneficiary
        type: string
      nickname:
        description: Nickname of beneficiary
        type: string
      address:
        $ref: '#/definitions/address'
      image-url:
        description: URL of beneficiary image kept in content repository
        type: string
      is-trusted:
        description: Whether beneficiary is trusted or not
        type: boolean
      accounts:
        description: Accounts
        items:
          $ref: '#/definitions/account-info'
        type: array
      identifiers:
        description: Identifiers.
        items:
          $ref: '#/definitions/party-identifier'
        type: array
    type: object
  modify-beneficiary-command:
    description: Command to modify personal benefitiary
    required:
      - name
      - is-trusted
    type: object
    properties:
      name:
        description: Name of beneficiary
        type: string
      nickname:
        description: Nickname of beneficiary
        type: string
      address:
        $ref: '#/definitions/address'
      image-url:
        description: URL of beneficiary image kept in content repository
        type: string
      is-trusted:
        description: Whether beneficiary is trusted or not
        type: boolean
      accounts:
        description: Accounts
        items:
          $ref: '#/definitions/account-info'
        type: array
      identifiers:
        description: Identifiers.
        items:
          $ref: '#/definitions/party-identifier'
        type: array
  purpose-code-list:
    description: List of purpose codes.
    example:
      total-count: -1
      page-size: 10
      page-number: 0
      total-pages: 0
      sort-order: desc
      sort-by:
      items:
        - code: '102'
          code-description: 'Descrease of deposit on account of nonresident in cash and in RSD.'
          documents:
            - invoice
          code-type: domestic
    allOf:
    - $ref: '#/definitions/paged-list'
    properties:
      items:
        description: Purpose codes.
        items:
          $ref: '#/definitions/purpose-code'
        type: array
    type: object
  purpose-code:
    description: Purpose code for transfer.
    example:
      code: "289"
      code-description: Payment of citizen.
      document-list: sample
      code-type: domestic
    properties:
      code:
        description: Purpose code.
        type: string
      code-description:
        description: Purpose description.
        type: string
      documents:
        description: List of necessary documents.
        items:
          type: string
        type: array
      code-type:
        description: Type of purpose code
        enum: [domestic, cross-border, sepa]
        type: string
    type: object
  currency:
    description: Currency amount and code.
    example:
      code: EUR
      amount: 0
    properties:
      code:
        description: Code of currency.
        type: string
      amount:
        description: Amount.
        format: double
        type: number
    type: object

  media:
    type: object
    description: URLs of images attached to transaction
    properties:
      image:
        description: URL of transaction image
        type: string
        format: url
      party-image:
        description: URL of image representing other party in transaction
        type: string
        format: url

  geolocation:
    type: object
    required:
      - lat
      - long
    description: The geographic coordinates of a location
    properties:
      lat:
        description: The latitude of a location. For example 37.42242
        format: double
        type: number
      long:
        description: The longitude of a location. For example -122.08585
        format: double
        type: number
      mapped-address:
        description: Address of location specified with coordinates
        type: string

  nonresident-transfer-instruction:
    description: Instruction with details of nonresident transfer
    type: object
    allOf:
      - $ref: '#/definitions/cross-border-transfer-instruction'

  attachment:
    description: Content sent along with message, consists of a type of file, name and content.
    properties:
      content-type:
        description: |
          The type of content that is sent as an attachment.
          For a list of possible values see [content-types](payment-classifications.html#content-types) enumeration.
        enum:
          - image/png
          - image/jpg
          - image/tiff
          - application/pdf
          - application/msword
          - application/vnd.ms-excel
        type: string
      name:
        description: Attachment name
        type: string
      id:
        description: Unique identifier of attachment
        type: string
      content-url:
        description: URL where content of attachment is available
        type: string
        format: uri
    type: object
  address:
    type: object
    description: Postal address
    required:
      - address-line
      - locality
    properties:
      address-line:
        description: Address line with street name and number
        type: string
      locality:
        description: Populated place such as city, town or village
        type: string
      country:
        description: "ISO3166 2 letter code of country. List of countries is maintained by [Reference API](reference-getstarted.html)."
        type: string
        maxLength: 2
        minLength: 2
  transfer:
    description: Transfer details
    type: object
    properties:
      transfer-id:
        description: Unique identifier of transfer assigned by bank
        type: string
      status:
        description: |
          Transfer status
          For a list of possible values see [transfer-statuses](payment-classifications.html#transfer-statuses) enumeration.
        type: string
        enum:
          - pending
          - scheduled
          - executed
          - rejected
          - canceled
      status-date:
        description: Date and time when the transfer status was reached
        format: date-time
        type: string
      originated:
        description: Date and time when the transfer was originated
        format: date-time
        type: string
      instruction:
        $ref: '#/definitions/instruction'
      transfer-kind:
        description: |
          Type of the transfer
          For a list of possible values see [transfer-kinds](payment-classifications.html#transfer-kinds) enumeration.
        enum:
          - balance-transfer
          - credit-transfer
          - cross-border-transfer
          - nonresident-transfer
          - p2p-transfer
          - topup
          - bill-payment
        type: string
      direction:
        description: |
          Direction of transfer from customer's perspective (debit, credit).
          For a list of possible values see [transfer-directions](payment-classifications.html#transfer-directions) enumeration.
        enum:
          - inpayment
          - outpayment
          - internal
        type: string
    required:
      - transfer-id
    example:
      transfer-id: "1283724237"
      status: scheduled
      status-date: 2016-02-01T01:25:00
      originated: 2016-03-01T01:25:00
      instruction:
        kind: credit-transfer
      transfer-kind: balance-transfer
      direction: inpayment

  instruction:
    description: Transfer instruction
    type: object
    discriminator: kind
    properties:
      kind:
        description: |
          Transfer kind
          For a list of possible values see [transfer-kinds](payment-classifications.html#transfer-kinds) enumeration.
        type: string
        enum:
          - balance-transfer
          - credit-transfer
          - cross-border-transfer
          - nonresident-transfer
          - p2p-transfer
          - topup
          - bill-payment
          - wallet-transfer
      media:
        $ref: '#/definitions/media'
      geolocation:
        $ref: '#/definitions/geolocation'
    required:
      - kind
  balance-transfer-instruction:
    description: Instruction with details of balance transfer
    type: object
    allOf:
      - $ref: '#/definitions/instruction'
    properties:
      creditor-account:
        description: Creditor account number
        type: string
      debtor-account:
        description: Debtor account number
        type: string
      instructed-amount:
        description: Currency amount.
        $ref: '#/definitions/currency'
    required:
      - creditor-account
      - debtor-account
      - instructed-amount
  p2p-transfer-instruction:
    description: Instruction with details of person to person transfer
    type: object
    allOf:
      - $ref: '#/definitions/instruction'
    properties:
      debtor-name:
        description: Name of the debtor
        type: string
      debtor-address:
        description: Address of the debtor
        $ref: '#/definitions/address'
      creditor-name:
        description: Name of the creditor.
        type: string
      debtor-account-number:
        description: Debtor account number.
        type: string
      creditor-identifier:
        description: Identifier of the creditor
        type: string
      creditor-identity-kind:
        description: |
          Kind of the identifier of the creditor used to deliver p2p payment.
          For a list of possible values see [p2p-party-kinds](payment-classifications.html#p2p-party-kinds) enumeration.
        enum:
          - email
          - msisdn
          - facebook
        type: string
      instructed-amount:
        $ref: '#/definitions/currency'
      debtor-message:
        description: Message sent with the payment.
        type: string
    required:
      - debtor-name
      - debtor-account-number
      - creditor-name
      - creditor-identifier
      - creditor-identity-kind
      - instructed-amount
  credit-transfer-instruction:
    description: Instruction with details of credit transfer
    allOf:
      - $ref: '#/definitions/balance-transfer-instruction'
    properties:
      debtor-name:
        description: Name of the debtor
        type: string
      debtor-address:
        description: Address of the debtor
        $ref: '#/definitions/address'
      creditor-name:
        description: Name of the creditor
        type: string
      creditor-address:
        description: Address of the creditor
        $ref: '#/definitions/address'
      payment-description:
        description: Payment description.
        type: string
      purpose-code:
        description: |
          Classification that explains purpose of transfer. List of purpose codes is maintained by [Reference API](reference-getstarted.html).
        type: string
      urgency:
        description: |
          Urgency of transfer
          For a list of possible values see [transfer-urgencies](payment-classifications.html#transfer-urgencies) enumeration.
        enum:
          - ach
          - rtgs
        type: string
      instrument-code:
        description: Classifcation that explains kinds of instrument used acording to payment operational rules and regulations
        type: string
      debtor-reference:
        description: Debtor reference such as purchase order
        type: string
      debtor-reference-model:
        description: Optional debtor reference model that specifies check digit algorithm used to verify debtor reference
        type: string
      creditor-reference:
        description: Creditor reference such as invoice or bill number
        type: string
      creditor-reference-model:
        description: Optional debtor reference model that specifies check digit algorithm used to verify creditor reference
        type: string
      value-date:
        description: Date when transfer is expected to be executed
        format: date-time
        type: string
      funds-reservation:
        description: Indicates if funds for transfer are going to be reserved by bank to secure execution on value date
        type: string
        enum: [none, immediate]
    required:
      - creditor-name
      - debtor-name
      - payment-description
      - purpose-code
      - urgency
      - instrument-code
      - value-date
    type: object
  cross-border-transfer-instruction:
    description: Instruction with details of cross-border transfer
    type: object
    allOf:
      - $ref: '#/definitions/balance-transfer-instruction'
    properties:
      debtor-name:
        description: Name of the debtor
        type: string
      debtor-address:
        description: Address of the debtor
        $ref: '#/definitions/address'
      creditor-name:
        description: Name of the creditor
        type: string
      creditor-address:
        description: Address of the creditor
        $ref: '#/definitions/address'
      creditor-reference:
        description: Creditor reference such as invoice or bill number
        type: string
      charge:
        description: |
          The party that is charged for the transfer fee.
          For a list of possible values see [charged-parties](payment-classifications.html#charged-parties) enumeration.
        enum:
          - ordering-customer
          - shared
          - beneficiary
        type: string
      payment-description:
        description: Payment description.
        type: string
      purpose-code:
        description: |
          Classification that explains purpose of transfer. List of purpose codes is maintained by [Reference API](reference-getstarted.html).
        type: string
      value-date:
        description: Date when transfer is expected to be executed
        format: date-time
        type: string
      funds-reservation:
        description: Indicates if funds for transfer are going to be reserved by bank to secure execution on value date
        type: string
        enum: [none, immediate]        
      attachments:
        description: Attached documents required by cross-border payment regulations
        items:
          $ref: '#/definitions/attachment'
        type: array
      creditor-bank:
        description: Bank that receives the money in name of creditor
        type: string
      creditor-bank-address:
        $ref: '#/definitions/address'
      debtor-bank:
        description: Bank that sends money in the name of debtor
        type: string
      debtor-bank-address:
        $ref: '#/definitions/address'
      creditor-bank-swift-code:
        type: string
        description: Bank swift code
      residence-code:
        description: Residence code
        type: string
    required:
      - creditor-name
      - debtor-name
      - creditor-bank
      - creditor-bank-address
      - payment-description
      - purpose-code
      - value-date
      - residence-code
  bill-payment-instruction:
    description: Instruction with details for bill payment
    type: object
    allOf:
      - $ref: '#/definitions/instruction'
    properties:
      bill-id:
        description: Unique identifier of the bill to be settled
        type: string
    required:
      - bill-id
  create-draft-command:
    description: Command provides parameters for creating new template
    type: object
    properties:
      instruction:
        $ref: '#/definitions/instruction'
      transfer-kind:
        description: |
          Type of the transfer
          For a list of possible values see [transfer-kinds](payment-classifications.html#transfer-kinds) enumeration.
        enum:
          - balance-transfer
          - credit-transfer
          - cross-border-transfer
          - nonresident-transfer
          - p2p-transfer
          - topup
          - bill-payment
        type: string
      is-template:
        description: wheather draft is used as template
        type: boolean
      name:
        type: string
        description: Customer assigned name of the template
    required:
      - name
      - is-template
      - transfer-kind
  modify-draft-command:
    description: Command provides parameters for modifying existing template.
    properties:
      instruction:
        $ref: '#/definitions/instruction'
      transfer-kind:
        description: |
          Type of the transfer
          For a list of possible values see [transfer-kinds](payment-classifications.html#transfer-kinds) enumeration.
        enum:
          - balance-transfer
          - credit-transfer
          - cross-border-transfer
          - nonresident-transfer
          - p2p-transfer
          - topup
          - bill-payment
        type: string
      is-template:
        description: wheather draft is used as template
        type: boolean
      name:
        type: string
        description: Customer assigned name of the template
    required:
      - name
      - is-template
      - transfer-kind
  draft-list:
    description: List of drafts and templates
    allOf:
    - $ref: '#/definitions/paged-list'
    properties:
      drafts:
        description: Transfer drafts and templates
        items:
          $ref: '#/definitions/draft'
        type: array
    type: object
    example:
      total-count: 455
      page-size: 10
      page-number: 1
      total-pages: 46
      items:
      -
        instruction:
          kind: credit-transfer
        draft-id: fs34r334rf34
        created: 2016-02-01T01:25:00
        transfer-kind: balance-transfer
        is-template: false
        name: name1
      -
        instruction:
          kind: p2p-transfer
        draft-id: 345re3f3f
        created: 2016-02-01T01:55:00
        transfer-kind: nonresident-transfer
        is-template: true
        name: name2
  draft:
    description: Draft or template for transfer
    type: object
    properties:
      draft-id:
        description: Unique identifier of draft
        type: string
        readOnly: true
      created:
        description: Date and time when the draft or template was created
        format: date-time
        type: string
        readOnly: true
      instruction:
        $ref: '#/definitions/instruction'
      transfer-kind:
        description: |
          Type of the transfer
          For a list of possible values see [transfer-kinds](payment-classifications.html#transfer-kinds) enumeration.
        enum:
          - balance-transfer
          - credit-transfer
          - cross-border-transfer
          - nonresident-transfer
          - p2p-transfer
          - topup
          - bill-payment
        type: string
        readOnly: true
      is-template:
        description: wheather draft is used as template
        type: boolean
        default: true
      name:
        type: string
        description: Customer assigned name of the template
    required:
      - instruction
    example:
      instruction:
        kind: credit-transfer
      draft-id: sdf3jku342
      created: 2016-02-01T01:25:00
      transfer-kind: balance-transfer
      is-template: false
      name: name1

  set-transfer-image-url-command:
    description: Command with image url to set
    properties:
      image-url:
        description: Transfer's image url kept in [Content API](content-getstarted.html)
        type: string
    type: object
  set-transfer-geolocation-command:
    description: Command with  geolocation to set
    properties:
      geolocation:
        description: Geographic coordinates and optional mapped adress
        $ref: '#/definitions/geolocation'
    type: object
  execute-p2p-transfer-instruction:
    description: Command provides parameters for executing person to person transfer.
    properties:
      creditor-name:
        description: Name of the creditor
        type: string
      creditor-address:
        description: Addres of the creditor
        $ref: '#/definitions/address'
      creditor-account:
        description: Creditor account number
        type: string
      marketing-consent:
        description: Consent of the recipient to receive marketing messages from the bank
        type: boolean
      instructed-amount:
        description: Currency amount.
        $ref: '#/definitions/currency'
      collection-method:
        description: |
          Specifies the type of p2p transfer collection method
          For a list of possible values see [collection-methods](payment-classifications.html#collection-methods) enumeration.
        enum:
          - internal
          - external
          - atm
        type: string
      collection-code:
        description: Collection code. It must match the one generated by the system when P2P was initiated.
        type: string
    required:
      - creditor-name
      - creditor-address
      - creditor-account
      - collection-method
    type: object
  top-up-transfer-instruction:
    description: Command provides parameters for initiating topup transfer.
    type: object
    allOf:
      - $ref: '#/definitions/instruction'
    properties:
      debtor-name:
        description: Name of the debtor
        type: string
      debtor-address:
        description: Address of the debtor
        $ref: '#/definitions/address'
      debtor-account:
        description: Debtor account number
        type: string
      operator-code:
        description: Code of the mobile operator.
        type: string
      msisdn:
        description: Phone number to credit
        type: string
      instructed-amount:
        description: Currency amount.
        $ref: '#/definitions/currency'
    required:
      - debtor-name
      - debtor-account
      - operator-code
      - msisdn

  wallet-transfer-instruction:
    description: Command provides parameters for initiating wallet transfer
    type: object
    allOf:
      - $ref: '#/definitions/instruction'
    properties:
      customer-number:
        description: Unique identifier of the customer that is wallet holder
        type: string
      wallet-number:
        description: Unique identifier of the wallet
        type: string
      linked-account:
        description: Unique identifier of transactional account linked to wallet
        type: string
      instructed-amount:
        description: Currency amount.
        $ref: '#/definitions/currency'
      direction:
        type: string
        enum: [top-up, withdrawal]
    required:
      - customer-number
      - wallet-number
      - linked-account
      - instructed-amount
      - direction
    example:
      kind: wallet-transfer
      customer-number: nodjo0001
      wallet-number: MW008819020
      linked-account: "361003000091091095"
      instructed-amount:
        amount: 5000.00
        code: RSD
      direction: top-up

  transfer-list:
    description: List of transfers.
    allOf:
    - $ref: '#/definitions/paged-list'
    properties:
      items:
        description: Transfer list.
        items:
          $ref: '#/definitions/transfer'
        type: array
    type: object
    example:
      total-count: 455
      page-size: 10
      page-number: 1
      total-pages: 46
      items:
      -
        transfer-id: "234234718"
        status: scheduled
        status-date: 2016-02-01T01:25:00
        originated: 2016-02-01T01:55:00
        instruction:
          kind: credit-transfer
        transfer-kind: balance-transfer
        direction: inpayment
      -
        transfer-id: "2342797204"
        status: pending
        status-date: 2016-02-01T01:25:00
        originated: 2016-02-01T01:45:00
        instruction:
          kind: p2p-transfer
        transfer-kind: nonresident-transfer
        direction: outpayment
  incoming-cross-border-transfer-list:
    description: List of incomes.
    allOf:
    - $ref: '#/definitions/paged-list'
    properties:
      items:
        description: Income list.
        items:
          $ref: '#/definitions/incoming-cross-border-transfer'
        type: array
    type: object
    example:
      total-count: 455
      page-size: 10
      page-number: 1
      total-pages: 46
      items:
      - transfer-id: "42349827394"
        instruction:
          kind: credit-transfer
        status: pending
        status-date: 2016-02-01T01:25:00
      - transfer-id: "423487217319"
        instruction:
          kind: p2p-transfer
        status: executed
        status-date: 2016-02-01T01:25:00
  incoming-cross-border-transfer:
    description: Income details.
    type: object
    properties:
      transfer-id:
        description: Unique identifier of transfer assigned by bank
        type: string
      instruction:
        $ref: '#/definitions/cross-border-transfer-instruction'
      status:
        description: |
          Transfer status
          For a list of possible values see [incoming-cross-border-transfer-statuses](payment-classifications.html#incoming-cross-border-transfer-statuses) enumeration.
        type: string
        enum:
        - pending
        - executed
      status-date:
        description: Date and time when the transfer status was reached
        format: date-time
        type: string
    required:
      - transfer-id
  working-day:
    description: Get date of next working day during holdays
    properties:
      specified-date:
        description: Date from which next day is calculated
        format: date
        type: string
      next-working-date:
        description: Date of next working day for bank relative to specified date
        format: date
        type: string
    type: object
    example:
      specified-date: 2016-02-01
      next-working-date: 2016-03-01

# ****************************************************#
# MODELS COMMON WITH OTHER APIs                       #
# ****************************************************#
  validation-problem:
    type: array
    items:
      type: object
      properties:
        field:
          type: string
        errors:
          type: array
          items:
            type: object
            properties:
              error:
                type: string
              message:
                type: string
    example:
      -
        field: phone-number
        errors:
          -
            error: invalid-format
            message: Format for this field is invalid
          -
            error: max-length
            message: Content exceeds maximum alowed length
      -
        field: account-number
        errors:
          -
            error: check-digit-invalid
            message: Check digit is invalid for this field
  business-problem:
    description: Details on specific problem that prevented processing after successfull validation
    type: object
    example:
      problem: document-locked
      message: Document you are trying to access is locked by another user
      details: User john.doe has locked the document
    properties:
      problem:
        description: Unique literal that identifies specific problem
        type: string
      message:
        description: Message explaining the situation and optionaly remedies
        type: string
      details:
        description: Optional details supplied for troubleshooting
        type: string
  paged-list:
    description: List with support for paging
    properties:
      total-count:
        description: Total number of items in collection
        type: integer
      page-size:
        description: Size of the page
        type: integer
      page:
        description: Index of current page
        type: integer
      total-pages:
        description: Total number of pages of set size
        type: integer
      sort-order:
        description: Sort order (`asc` or `desc`). Default is asc
        type: string
        enum:
        - asc
        - desc
      sort-by:
        description: Attribute of the collection item to sort by
        type: string
  classification-value:
    type: object
    description: Classification value
    properties:
      literal:
        description: Literal that uniquely identifies classification value
        type: string
      code:
        description: Optional numerical code of classification value
        type: string
      description:
        description: Description of classification value
        type: string
      parent-literal:
        description: Optional literal of parent classification value if schema is hierarchical
        type: string
    example:
      literal: billing
      code: "2"
      description: Contact may be used to provide party with details of their liabilities to the bank
  classification-list:
    description: List of classification schemas
    type: object
    properties:
      items:
        description: List of classification schemas
        items:
          $ref: "#/definitions/classification-info"
        type: array
    example:
      -
        schema-id: filing-purpose
        name: Filing Purpose
        description: List of possible uses of document in business context. Commonly used filing purposes are document, customer-picture
      -
        schema-id: folder-purpose
        name: Folder Purpose
        description: List of possible uses of a folder in business context.
  classification-info:
    description: Basic information on classification schema
    properties:
      schema-id:
        type: string
        description: Unique literal that identifies the classification schema. Always in `lowercase-dash` convention.
      name:
        type: string
        description: User friendly name of classification
      description:
        type: string
        description: Description of a classification
    example:
      schema-id: filing-purpose
      name: Filing Purpose
      description: List of possible uses of document in business context. Commonly used filing purposes are document, customer-picture
  classification:
    type: object
    description: Classification schema details
    properties:
      schema-id:
        type: string
        description: Unique literal that identifies the classification schema. Always in `lowercase-dash` convention.
      name:
        type: string
        description: User friendly name of classification
      description:
        type: string
        description: Description of a classification
      values:
        description: List of allowed classification values
        type: array
        items:
          $ref: "#/definitions/classification-value"
    example:
      schema-id: contact-purpose
      name: Contact Purpose
      description: List of possible uses of contact in business context. Commonly used filing purposes are billing, marketing
      values:
        - literal: billing
          code: "1"
          description: Contact may be used to provide party with details of their liabilities to the bank
        - literal: marketing
          code: "2"
          description: Contact may be used to provide party with details of offers
        - literal: reporting
          code: "3"
          description: Contact may be used to provide party with reports on their account
  resource-created-result:
    description: Unique identifier of newly created resource that can be used in future inquires and commands
    type: string
    example: "288765"
responses:
  default-error-response:
    description: Besides specific status codes above, other standard http [status codes](http://www.w3.org/Protocols/rfc2616/rfc2616-sec10.html) can be returned for each request. To learn more see general guidance on [error handling](common-getstarted.html#error-handling)
  validation-problem-response:
    description: Your request was not constructed properly. Response contains the list of validation errors for each invalid element of your request. For complete list of possible validation errors see [validation](common-getstarted.html#validation) section of the guide.
    schema:
      $ref: '#/definitions/validation-problem'
